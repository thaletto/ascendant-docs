# Vercel runs `npm install`, whose strict peer resolution fails on the
# @vitejs/plugin-react -> @babel/core 8 optional-peer chain.
# Accept the same lenient resolution Bun uses locally.
legacy-peer-deps=true
